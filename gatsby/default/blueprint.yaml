version: 1

meta:
  name: "Gatsby"
  description: "Base Gatsby project"
  framework: "gatsby"
  create_command: "npm create gatsby@latest"

tasks:
  dev:
    - desc: "Start development server"
      run: "npm run develop"
  build:
    - desc: "Build for production"
      run: "npm run build"
  serve:
    - desc: "Serve production build"
      run: "npm run serve"
  clean:
    - desc: "Clean build artifacts"
      run: "npm run clean"
